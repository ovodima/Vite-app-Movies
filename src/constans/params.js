const settingsSlider = {
    
        className: "center",
        centerMode: true,
        infinite: true,
        centerPadding: "40px",
        rows:2,
        slidesToShow: 5,
        swipeToSlide: true,
        speed: 600,
    
}

export default settingsSlider